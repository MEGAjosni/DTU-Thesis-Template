%------------------------------------------------------------
% Encoding and Fonts
%------------------------------------------------------------
%\usepackage[utf8]{inputenc} % (only for pdfLaTeX; not needed with fontspec/XeLaTeX)
\usepackage{fontspec}           % For custom fonts

%------------------------------------------------------------
% Language and Localization
%------------------------------------------------------------
\usepackage[english]{babel}     % Language support
\usepackage{csquotes}           % Context-sensitive quotation support (used with biblatex)

%------------------------------------------------------------
% Document Layout and Appearance
%------------------------------------------------------------
\usepackage[bottom=3cm, top=3cm, left=3cm, right=3cm]{geometry} % Page margins
\usepackage{setspace}           % Line spacing
\usepackage{parskip}            % Space between paragraphs instead of indents
\usepackage{fancyhdr}           % Custom headers and footers
%\usepackage{microtype}         % Better typography (uncomment if using pdfLaTeX)

%------------------------------------------------------------
% Colors and Graphics
%------------------------------------------------------------
\usepackage{xcolor}             % Custom colors
\usepackage{graphicx}           % Including images
\usepackage{wrapfig}            % Wrap text around figures
\usepackage{float}              % Improved float control
\usepackage{caption}            % Better captions
\usepackage{subcaption}         % Subfigures
\usepackage{lscape}             % Landscape figures or pages

%------------------------------------------------------------
% Tables
%------------------------------------------------------------
\usepackage{tabularx}           % Better table column control
\usepackage{booktabs}           % Nicer horizontal rules in tables

%------------------------------------------------------------
% Mathematics and Symbols
%------------------------------------------------------------
\usepackage{amsmath, amssymb, bm}  % Math symbols
\DeclareMathOperator*{\argmin}{arg\,min}
\usepackage{siunitx}            % SI units
\usepackage{textcomp}           % Additional text symbols, e.g., \textdegree

%------------------------------------------------------------
% Code and Algorithms
%------------------------------------------------------------
\usepackage{listings}           % Code listings
\usepackage{algorithm}          % Algorithm float
\usepackage{algpseudocode}      % Pseudocode

%------------------------------------------------------------
% Cross-referencing and Links
%------------------------------------------------------------
\PassOptionsToPackage{hyphens}{url} % Allow URL breaks at hyphens
\usepackage{hyperref}           % Hyperlinks (loads url package)
\usepackage{cleveref}           % Smarter references

%------------------------------------------------------------
% TikZ and PGFPlots (Figures, Graphs, and Drawing)
%------------------------------------------------------------
\usepackage{tikz}               % Drawing and diagrams
\usetikzlibrary{shapes.geometric, matrix, positioning, fit, backgrounds, automata, spy}
\usepackage{pgfplots}           % Plots and graphs
\usepgfplotslibrary{groupplots, external}
\pgfplotsset{compat=newest}     % Use the latest version of PGFPlots
\tikzexternalize[prefix=build/figures/] % Externalize TikZ figures (compiled separately)

%------------------------------------------------------------
% Utilities and Miscellaneous
%------------------------------------------------------------
\usepackage{titlesec}           % Section title formatting
\usepackage{calc}               % Do arithmetic with lengths
\usepackage{blindtext}          % Placeholder text

%------------------------------------------------------------
% Bibliography
%------------------------------------------------------------
\usepackage[backend=biber,style=numeric,sorting=none,maxnames=1]{biblatex}
\addbibresource{references.bib}
